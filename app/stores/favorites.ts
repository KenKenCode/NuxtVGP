import {defineStore} from 'pinia'
export const userFavoritesStore = defineStore('favorites', {
    state: () => ({
        favorites: [] as Array<any>,
    }),

    actions: {
        addFavorite(rocket: any) {
            if 
        }
    }
})